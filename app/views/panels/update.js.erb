$("#<%= dom_id @panel %>").remove();
$(".workspace-inner").append("<%= j render partial: "/editor/panel", locals: {panel: @panel} %>");
if ($(".panel.selected").length == 0){
	$("#<%= dom_id @panel %>").addClass("selected");
}

window.app.bind_panels();