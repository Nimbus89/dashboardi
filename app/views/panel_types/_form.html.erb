<%= form_for(@panel_type) do |f| %>
  <% if @panel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panel_type.errors.count, "error") %> prohibited this panel_type from being saved:</h2>

      <ul>
      <% @panel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <%= f.fields_for :panel_fields do |builder| %>
    <%= render 'panel_field_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add Panel Field", f, :panel_fields %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
